# Frontend Environment Configuration
# Note: Firebase credentials are stored in server/.env
# All Firebase operations are proxied through the server API

# API Configuration
VITE_API_URL=http://localhost:5000/api

# Optional: Firebase Client Configuration (only if direct client-side Firebase is needed)
# Copy these from server/.env if you want client-side Firebase features
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
